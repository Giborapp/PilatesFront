{
  "framework": "nextjs",
  "installCommand": "corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @pilates-manager/web build",
  "outputDirectory": "apps/web/.next"
}
